import 'package:flutter/material.dart';
class Category_String extends StatelessWidget {
  const Category_String({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Category',
      style: TextStyle(
          fontWeight: FontWeight.w400,
          fontSize: 24.0
      ),);
  }
}
